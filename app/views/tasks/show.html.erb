<h1>Info of task</h1>

<p>Title: <%= @task.title %></p>
<p>Details: <%= @task.details %></p>
<p>Completed? <%= if @task.completed = "Yes" then "No, shame on you" end %></p>

<%= link_to "Remove this task", delete_path(@task.id),  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>

<%= link_to "Back to list", tasks_path %>
